name: RETIRED - TMP AgentNDX DELTA submission 20260911

# One-shot free submission completed on 2026-09-11 through AgentNDX's official
# /api/submit form endpoint. The server returned HTTP 302 Location:
# /submit?success=1 after a dedupe readback showed no existing DELTA entry.
# Listing state remains review_pending until AgentNDX publishes it.
# Do not resubmit or create a duplicate listing.
# Current owner pricing remains Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'AgentNDX submission accepted; one-shot workflow retired.'
