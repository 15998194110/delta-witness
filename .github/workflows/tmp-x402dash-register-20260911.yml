name: RETIRED - TMP x402dash DELTA registration 20260911

# One-shot evidence run confirmed all three canonical DELTA endpoints already exist
# in x402dash. No duplicate registration write was performed. Original run evidence
# remains in GitHub Actions history.
# Current owner prices: Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Canonical x402dash listings confirmed; one-shot probe retired.'
