name: RETIRED - TMP PayanAgent DELTA bounded retry 20260911

# Completed successfully on 2026-09-11 after policy/live-price verification and
# independent Base Treasury reconciliation. PayanAgent public discovery already
# contained canonical or named DELTA, so duplicate seller/listing creation was
# suppressed. Sanitized evidence is in docs/evidence/payanagent-2026-09-11.json.
# Current owner pricing: Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PayanAgent distribution check complete; duplicate write suppressed; one-shot retired.'
