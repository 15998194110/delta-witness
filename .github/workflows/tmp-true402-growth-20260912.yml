name: RETIRED - TMP DELTA true402 growth run 20260912

# Retired after migration into the canonical discovery pulse.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; canonical discovery pulse owns the active growth check.'
