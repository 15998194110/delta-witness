name: RETIRED - true402 canonical registration 20260911

# One-shot completed its only external write on 2026-09-11 Asia/Shanghai:
# true402 registration returned HTTP 201 after current owner pricing verification.
# Immediate catalog readback was not yet visible, so visibility remains a recoverable
# deferred recheck for the existing discovery loop. Do not replay this registration
# or create a duplicate seller/service identity. Historical Actions logs are preserved.
# Current owner ladder: Capture/Partner/Watch=1; Public Preflight=5; Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot retired; no external write is performed.'
