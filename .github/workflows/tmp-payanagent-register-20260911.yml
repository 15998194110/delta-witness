name: RETIRED - TMP PayanAgent canonical DELTA registration 20260911

# First one-shot failed locally before any PayanAgent write because the inline Node
# program mixed CommonJS require() with top-level await. Pricing verification and
# independent Treasury reconciliation completed; no PayanAgent registration was
# attempted. Retired before a corrected bounded retry to avoid duplicate loops.
# Current owner pricing: Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Failed pre-write one-shot retired; no external listing write occurred.'
