name: RETIRED - TMP Aegis DELTA submission 20260911

# Retired after live probing showed Aegis /submit now requires a $0.05 USDC
# x402 payment per listing. Owner policy forbids autonomous listing/signup fees.
# No payment was authorized or sent; no DELTA listing was created by this probe.
# Current owner pricing remains Capture=1, Preflight=5, Pilot=10 USDC.
# Original execution evidence remains in Git/Actions history.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Aegis listing probe retired. Paid submission requires separate exact authorization.'
