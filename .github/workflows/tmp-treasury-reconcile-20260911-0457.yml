name: TMP DELTA treasury reconcile 20260911 0457

on:
  push:
    paths:
      - .github/workflows/tmp-treasury-reconcile-20260911-0457.yml

permissions:
  contents: read

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Reconcile Base Treasury against known DELTA classifications
        run: node scripts/reconcile-treasury.mjs
