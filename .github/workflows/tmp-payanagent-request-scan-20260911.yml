name: RETIRED - TMP PayanAgent open request scan 20260911

# Completed one public request scan. 60 open requests were observed; keyword
# candidates had no explicit funded escrow evidence, so no bid was authorized.
# Evidence is retained in docs/evidence/payanagent-open-requests-2026-09-11.json.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Request scan complete; no A-grade funded compatible request; one-shot retired.'
