name: RETIRED - DELTA x402 discovery compatibility one-shot
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Discovery compatibility phase completed; further payment-route changes require a separate reviewed deployment path.'
