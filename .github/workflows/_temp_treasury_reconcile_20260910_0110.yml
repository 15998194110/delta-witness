name: DELTA Treasury Reconcile 0110

on:
  push:
    paths:
      - '.github/workflows/_temp_treasury_reconcile_20260910_0110.yml'

permissions:
  contents: read

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - run: node scripts/reconcile-treasury.mjs
