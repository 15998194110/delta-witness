name: RETIRED - TMP Agent402 DELTA listing refresh 20260911

# One-shot free registration/refresh completed successfully on 2026-09-11.
# Agent402 returned listed=true for the canonical DELTA origin with
# toolCount=6, network eip155:8453, routable=true and health=1.
# No fee, signup, wallet signature or duplicate seller identity was created.
# Current owner pricing remains Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Agent402 canonical listing is live/routable; one-shot workflow retired.'
