name: RETIRED - Agent Bazaar DELTA Capture submission 20260913
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'DELTA Witness Capture submitted to Agent Bazaar for review; one-shot retired to prevent duplicates.'
