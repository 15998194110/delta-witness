name: RETIRED - DELTA run11 reconciliation 20260912

# One-shot read-only pricing gate completed on 2026-09-12.
# Live public x402 amounts verified successfully. The paid-funnel step lacked
# scoped Cloudflare credentials in this temporary runner, so it was isolated;
# Treasury reconciliation was immediately rerun independently in the companion
# treasury-only one-shot. This workflow is retired to prevent duplicate loops.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot reconciliation retired.'
