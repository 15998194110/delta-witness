name: Temporary DELTA run6 reconciliation
on:
  push:
    paths:
      - '.github/workflows/tmp-run6-reconcile-20260914.yml'
permissions:
  contents: read
jobs:
  treasury:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Reconcile Base Treasury independently after unrelated audit degradation
        run: node scripts/reconcile-treasury.mjs
