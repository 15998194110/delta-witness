name: DELTA Obsolete Price Restore - Retired

# The owner explicitly reaffirmed the 1 / 5 / 10 USDC ladder on 2026-09-10.
# This workflow previously asserted and deployed obsolete 0.03 public prices.
# Do not restore its push trigger or deployment steps from historical runs.
# Retained as an inert record, not as an active deployment path.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired. Follow AGENTS.md and docs/pricing-policy-2026-09-10.md. Capture/Partner/Watch=1; Public Preflight=5; Pilot=10 USDC.'
