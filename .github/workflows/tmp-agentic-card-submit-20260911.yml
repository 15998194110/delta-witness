name: RETIRED - TMP Agentic Card DELTA submission 20260911

# Probe completed without submission. Agentic Card is free/no-account and exposes
# POST /api/submit, but its official submit page explicitly states that submitting
# confirms agreement to its Terms of Service. Owner policy forbids autonomous legal
# commitments without separate exact authorization, so no listing was created.
# Public dedupe search showed no DELTA entry at probe time.
# Current owner pricing remains Capture=1, Public Preflight=5, Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Agentic Card probe retired at owner legal-commitment boundary.'
