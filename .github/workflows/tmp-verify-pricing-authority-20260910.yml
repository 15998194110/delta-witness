name: RETIRED - TMP Verify Pricing Authority 20260910

# Owner authorized task synchronization and deduplication on 2026-09-10.
# This temporary migration check is superseded by the existing Discovery Pulse
# source/live checks and Release Train full verification. Preserve their schedules.
# Original definition and run evidence remain in Git/Actions history.
# Do not re-enable this one-shot or create a new periodic pricing-repair loop.
# Current ladder: Capture/Partner/Watch=1; Public Preflight=5; Pilot=10 USDC.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded temporary check retired; existing verification is authoritative. No deployment or expansion is performed here.'
