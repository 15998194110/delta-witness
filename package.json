{
  "name": "delta-witness",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run",
    "funnel": "node scripts/query-funnel.mjs",
    "bazaar": "node scripts/check-bazaar.mjs",
    "verify": "npm run types && npm run check && npm test && npm run build"
  },
  "dependencies": {
    "@x402/core": "^2.0.0",
    "@x402/evm": "^2.0.0",
    "@x402/hono": "^2.0.0",
    "@x402/extensions": "^2.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "vitest": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
