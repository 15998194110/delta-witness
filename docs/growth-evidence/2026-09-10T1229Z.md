# DELTA Growth Evidence — 2026-09-10T12:29Z

## Core production integrity

Evidence grade: **A — direct production HTTP challenge + deployment verification**.

- Capture: `POST /v1/capture` -> HTTP 402, amount `30000` USDC base units = **$0.03**, network `eip155:8453`, `payTo=0x1990e21bc219696ff7fbc26527dbaed335ac6367`.
- Preflight: `POST /v1/preflight` -> HTTP 402, amount `30000` USDC base units = **$0.03**, network `eip155:8453`, same Treasury.
- Guarded-Action Pilot: `POST /v1/guarded-action-pilot` -> HTTP 402, amount `10000000` USDC base units = **$10.00**, network `eip155:8453`, same Treasury.
- Cloudflare Worker production version after repair: `95698e68-6b95-45b7-ba41-e09a0bf9fd2b`.
- Source verification before deploy: 11 test files / 57 tests passed.
- Pricing-secret collision check: `CAPTURE_BASE_PRICE_USD=false`, `PREFLIGHT_BASE_PRICE_USD=false`; configured runtime vars are authoritative.

The run began by detecting a real production pricing drift ($1 Capture instead of the owner-authorized $0.03). The failure was repaired rather than treated as terminal: repository policy, tests, runtime variables, and deployment were aligned, the Worker was redeployed, and all three production 402 challenges were re-verified.

## Treasury reconciliation

Evidence grade: **A — direct canonical Base USDC Transfer logs**.

- Treasury: `0x1990e21bc219696ff7fbc26527dbaed335ac6367`.
- Asset: canonical Base USDC `0x833589fcd6edb6e08f4c7c32d4f71b54bda02913`.
- Latest independently scanned Base block: `51126959`.
- Lookback: 50,000 blocks.
- Treasury receipt candidates: 1.
- Total received in window: $0.03 USDC.
- Known non-customer amount: $0.03 USDC.
- Unclassified amount: $0.00.
- Only receipt: tx `0xdac6c2ccc3857685b52dfdd18322dd3968d2f074a2d3ecc64434b9b4c7219fe9`, payer `0x7e6b6556322c4e26c567a867964ac793f5ee2b1c`, classified `payapi_platform_verification`, `customer_revenue=false`.
- Verified stranger/customer `treasury_received`: **$0.00**.
- Verified customer paid settlements: **0**.
- Verified customer revenue: **$0.00**.

A Base RPC HTTP 429 occurred during one log chunk and was handled as recoverable channel degradation; retry succeeded and the reconciliation completed independently.

## Distribution channels

### 402 Index

Evidence grade: **A — official registration API response + independent live 402 verification**.

All three canonical POST endpoints returned HTTP 201 from `402index.io/api/v1/register` with message `Service registered and live (domain verified).` The official service records are `status=active`, `health_status=healthy`, `probe_status=probeable`, `x402_payment_valid=1`, `x402_asset_known=1`, and `http_method=POST`.

- Capture service id `73b990f5-02f6-4ef4-9892-645349938b63`, price `$0.03`, reliability score `82`, official verifier observed HTTP 402 / amount `30000` / Base / current Treasury.
- Preflight service id `8632f21c-6981-488e-bff5-dc0d41055886`, price `$0.03`, reliability score `82`, official verifier observed HTTP 402 / amount `30000` / Base / current Treasury.
- Guarded-Action Pilot service id `e98fec92-fe53-47bc-b274-fe228479fe49`, price `$10.00`, reliability score `85`, official verifier observed HTTP 402 / amount `10000000` / Base / current Treasury.

Listing status: **active/live**. Discovery status: **healthy/probeable**. Buyer count: no independently verified buyer surfaced in this run. Settlement count: 0 verified customer settlements. Revenue: $0 customer revenue.

### Market402

Evidence grade: **A official submission receipt / B advisory instant probe**.

Corrected-price submissions were accepted for Capture `$0.03`, Preflight `$0.03`, and Pilot `$10`. Each fresh instant check passed **11/11** checks with verdict `spec_compliant`. Market402 explicitly states the instant check is advisory and its `402 Verified` badge only follows its own scheduled probe, so discovery verification remains pending and is not overclaimed.

### x402 Arena

Existing names for all three DELTA resources returned HTTP 409 `Agent name already taken`; interpreted as already-present state, not a new listing. No new buyer or revenue evidence was claimed.

### x402dash

Direct registration was attempted after the production repair. The channel's registration verifier issued GET-style checks against DELTA's POST-only paid routes and reported HTTP 404 rather than 402. Because the core POST routes independently verified healthy and correctly priced, this is isolated as a method-compatibility mismatch/deferred channel, not a DELTA production failure. No unsafe route-semantic change was made merely to satisfy the directory.

## Run metrics

- `external_requests` from independently verified buyers: **0**
- `402_intents` from independently verified buyers: **0**
- `paid_settlements` (customer): **0**
- `treasury_received` (customer): **$0.00**
- `revenue` (customer): **$0.00**
- `variable_cost` attributable to verified customer fulfillment this run: **$0.00 verified**
- `contribution_margin` attributable to verified customer fulfillment this run: **$0.00**
- `buyer_count`: **0 verified**
- `settlement_count`: **0 verified customer settlements**
- `verified_neighbor_demand`: not newly quantified in this run; no unsupported count asserted.
