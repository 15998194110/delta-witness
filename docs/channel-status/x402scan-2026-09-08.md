# x402scan distribution — 2026-09-08

Status: origin registration succeeded; three paid DELTA resources were registered from the production OpenAPI. This is a distribution event, not a buyer order or revenue event.

## Registration evidence

One-shot workflow run: `34228268035`, source commit `a5446ff69f1a7e51a2a85164a062ee3875454937`.

The official x402scan `public.resources.registerFromOrigin` procedure returned HTTP 200 with:

- `success=true`
- `registered=3`
- `failed=0`
- `source=openapi`
- `originId=d211a046-6a86-4a56-8c33-9f60b1536d8b`
- four additional public routes discovered (`/health`, `/v1/quote`, `/v1/proofs/{proof_id}`, `/v1/demo`)

The three protected routes are the existing DELTA Capture, public Preflight and Guarded Action Pilot endpoints. No new wallet signature, paid canary, listing fee, identity or product was created. The temporary registration workflow was removed immediately after success.

x402scan publicly describes its registration surface as a way to list APIs so agents can find and pay for them, and its discovery pages expose per-server transaction, volume and buyer activity. Registration therefore increases machine discovery reach, but a listing alone is not an external request, payment authorization, settlement or Treasury receipt.

## Revenue checkpoint

A separate main-Operator read-only reconciliation run `34230447974` completed at 2026-09-08 13:11 UTC. It scanned canonical Base USDC transfers to Treasury `0x1990e21bc219696ff7fbc26527dbaed335ac6367` from block `51040990` through `51041874` and returned `incoming_count=0`. The D1 interval from `2026-09-08 12:45:00` contained only `payment_required` probes and page views; there was no `payment_verified` or `capture_completed` event. These probes are not counted as buyers or revenue.

No new customer revenue is recognized in this checkpoint.

## Dependency-channel follow-up

Fresh market research identified Shopper (`shopper.sh`) as a strong downstream dependency candidate: it operates a shopping MCP with 52 tools, deep browser shopping, seller vetting, persistent Radar scans, and x402/USDC self-payment. Its official site publishes `hello@shopper.sh`. DELTA Guard could fit as a deterministic public-page observation before a consequential purchase or supplier action. No message was sent in this run because the Asia/Shanghai natural-day cold-email allowance had already reached three first contacts; do not bypass that cap. Re-verify the business address and prior-contact state before any later first contact.
