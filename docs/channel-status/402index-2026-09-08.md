# 402 Index direct registration — 2026-09-08 evening

Status: live / active on 402 Index. Evidence quality: A for listing and x402 challenge verification; no buyer or revenue claim.

A free, no-account direct `POST https://402index.io/api/v1/register` registration was executed for the three existing DELTA production paid routes. Each returned HTTP 201 on the first attempt and `Service registered and live (domain verified).` No listing fee, signup fee, wallet signature, asset movement, new product, or legal commitment was used.

| Route | 402 Index service id | submitted price | registry status | health | x402 validation |
|---|---|---:|---|---|---|
| Capture `/v1/capture` | `73b990f5-02f6-4ef4-9892-645349938b63` | 0.03 USDC | active | healthy | HTTP 402; Base `eip155:8453`; amount `30000`; canonical USDC; existing Treasury; `x402_payment_valid=1` |
| Public Preflight `/v1/preflight` | `8632f21c-6981-488e-bff5-dc0d41055886` | 1 USDC | active | healthy | HTTP 402; Base `eip155:8453`; amount `1000000`; canonical USDC; existing Treasury; `x402_payment_valid=1` |
| Guarded Action Pilot `/v1/guarded-action-pilot` | `e98fec92-fe53-47bc-b274-fe228479fe49` | 10 USDC | active | healthy | HTTP 402; Base `eip155:8453`; amount `10000000`; canonical USDC; existing Treasury; `x402_payment_valid=1` |

All three registry records use provider `DELTA Witness`, category `verification/web-proof`, source `self-registered`, reliability score 80, `probe_status=probeable`, and Treasury `0x1990e21bc219696ff7fbc26527dbaed335ac6367`. The registry verification itself created only unpaid 402 probes and must not be counted as external buyer intent or settlement.

Strict reconciliation was executed independently after registration. Canonical Base USDC inbound logs were scanned from block 51036340 through 51037097 using bounded RPC fallbacks; `incoming_count=0`. Remote D1 telemetry from the latest operator checkpoint through the scan contained `page_view` and `payment_required` rows only. There were no `payment_verified`, fulfillment/capture completion, or other paid-delivery rows. The 402 Index verification probes appear as unpaid `payment_required` events at 10:29:14 / 10:29:15 / 10:29:17 UTC and are excluded from buyer/revenue metrics.

Therefore this event changes distribution only: `listing_status=live`, `discovery_status=active/healthy`, buyer_count unchanged, settlement_count unchanged, paid_settlements unchanged, treasury_received unchanged, revenue unchanged. The historical 0.03 USDC receipt remains separately unclassified for sprint net-revenue purposes pending the existing payer/proof/actual-fee classification.
