# TOLL402 free listing — September 8 evening

Status: submitted and accepted for review, not verified, not a buyer order, not revenue.

The original public resource lookup for `delta-witness` returned0 results. Existing repository evidence and business-mail history showed no prior submission. The free listing form at https://toll402.com/submit was inspected together with its public client implementation. Browser state retrieval timed out; the form's own `POST /api/contact` endpoint was then used directly with the same name/email/message/empty-website payload structure. It returned HTTP200 and `{"ok":true}`. No account, signature, paid listing or paid verification purchase was made.

Submitted one existing API: DELTA Witness Preflight, `https://delta-witness-api.ruphussten.workers.dev/v1/preflight`,1 USDC on Base; existing business contact used for review. Included the existing public OpenAPI, a public example.com request with deterministic text expectation, original treasury and the existing public proof. No new product, ownership terms or delivery promise was introduced.

TOLL402 says review takes about a day and describes a platform-funded paid verification process. These are platform claims, not evidence of payment or guaranteed acceptance. Do not resubmit or report an active/verified listing. Next check no earlier than September9 evening unless a real review/payment event arrives. This is distinct from trytollbooth.com, whose service API outages do not describe toll402.com.

Original request/response evidence is in the existing Commercial Operator workspace at `evidence/toll402_submission_20260908.json`. Other evening checks: production Preflight remains1; Agent402 now displays1 with Bazaar0.03 conflict flagged, and agent-tools.cloud now displays1. PayanAgent's old seller offer remains0.03 and original-offer editing authority is not established. Agent402 maintainer's accurate earlier rollback diagnosis was acknowledged on the existing thread, without another refresh request.

Base canonical USDC scan51021963 through51036339 returned no incoming transfers. D1 returned no new paid-delivery events since the morning checkpoint. No new revenue is recognized; old0.03 receipt classification and actual-cost limits remain unchanged.
