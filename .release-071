DELTA 0.7.1 patch release marker — retry 2.
Purpose: publish the already-tested buyer-side MCP/SDK pricing fixes through the canonical trusted-publisher release workflow after correcting a stale literal-text release gate.
No pricing change. No customer payment. Remove after release readback.
