# DELTA Acquisition Engine v0.6

> CURRENT OWNER PRICING (2026-09-10): Capture / authenticated Partner Capture / authenticated Partner Preflight / each NEW Watch check = **1 USDC**; Public Preflight = **5 USDC**; Guarded-Action Pilot = **10 USDC**. Read `AGENTS.md` and `docs/pricing-policy-2026-09-10.md` before every run. Conflicting old prices, automatic repricing and paid-canary instructions below are superseded. Historical receipts and prepaid obligations are not rewritten.

## Current revenue-first expansion handoff — 2026-09-18 09:45 JST

Read `docs/channel-status/revenue-expansion-2026-09-18-0945.json` before another prospect contact, reply, bid or registry action. It is the newest repository handoff and supplements, but does not replace, the canonical Commercial Operator workspace.

- The latest canonical scheduled pulse completed successfully at `2026-09-18T00:24:58Z`. Owner-pricing checks, live price verification and the live purchase path all passed before demand/revenue checks. Paid-funnel candidates were **0**. Direct canonical-USDC Treasury reconciliation checked `2026-09-18T00:24:55.111Z`, covering Base blocks **51,400,872–51,450,871** (50,000 blocks), with **0 receipt candidates** and **0 unclassified receipts**. Strict sprint count remains **0 / 50** and **0 unique verified buyers**. This is explicit-range evidence, not an all-time zero assertion; realized variable costs and contribution margin remain unknown/null.
- AgentPact still has the three canonical active offers at **1 / 5 / 10 USDC**, **0 buyers**, **0 settlements** and **0 DELTA-native funded-compatible requests**. AgentExchange also has no compatible request. The current external funded scan shows **20 adjacent funded requests** but **0 DELTA-native funded requests**; do not bid merely because a budget exists.
- Five deduped, independently qualified first contacts were sent in this run: **Kissflow Procurement Cloud** (`sales@kissflow.com`), **Procr** (`hello@procr.io`), **Seloria** (`hello@seloria.io`), **GenLedge** (`hello@genledge.ai`), and **Delvion** (`hello@delvion.ai`). The common conversion ask is intentionally narrow: identify one real workflow plus one representative public supplier/product/vendor URL, then perform one genuine buyer-paid **1 USDC Capture**; use **5 USDC Public Preflight** only where an explicit expected-condition check is actually needed. These remain **Grade-C commercial demand** until a reply/funding event exists.
- **Delvion immediately hard-bounced with 550 5.1.1 user does not exist despite the address appearing on its current public site. Suppress `hello@delvion.ai`; do not retry or guess another address.** The other four new first contacts had no immediate bounce observed in the same mailbox window; that is delivery evidence only, not buyer interest.
- The inbox also showed an Akirolabs permanent access-denied bounce on `info@akirolabs.com`; suppress that address and do not retry unless a different current official route is independently verified.
- **Outlast AI** remains blocked on its explicit company-domain sender prerequisite. Do not spoof or bypass. **Robauto / AgentHub** remains awaiting reply after DELTA answered the founder's use-case question; do not chase silence. **Reap** remains a review acknowledgement only, not buyer intent.
- No fee, new cash/credit, wallet signature/message, asset movement, paid test, KYC/identity attestation, legal commitment, new seller identity or production deployment occurred in this run. Deadline stays **2026-09-21T13:43:28+09:00**.
- There is **no numerical daily outreach cap**. Scale only through independently qualified unique targets. Continue exact Gmail/operator dedupe, official public contacts only, one first contact per target, no chasing silence, and stop on rejection.

## Prior handoffs

All earlier handoffs under `docs/channel-status/revenue-expansion-2026-09-18-*.json` and `docs/channel-status/revenue-expansion-2026-09-17-*.json` remain audit evidence. In particular, `revenue-expansion-2026-09-18-0845.json` records Sievo, Sastrify and ProcureDesk plus the previous 100,000-block Treasury run; `0754.json` records GEP, JAGGAER APAC, Coupa Open Buy, Opstream, Zycus and Prokuria; `0509.json`, `0436.json`, `0330.json`, `0247.json` and `0152.json` preserve prior payment/procurement expansion, bounce corrections, Outlast/Robauto status and then-current reconciliations. Use the newest evidence when status conflicts and never rewrite historical receipts/exclusions.

North star: first **independent customer** payment, then repeatable CAC ≈ 0 channels.

Formula:

`Revenue = Discovery Surface × Qualified Intent × Payment Conversion × Delivery Success × Repeat Usage × ARPU`

The system expands Discovery Surface without adding sales labor.

## Revenue evidence invariant

A Base USDC transfer into Treasury is `treasury_received`, but it is not automatically customer revenue. Revenue is recognized only after the payer and DELTA proof/telemetry reconcile to a non-project, non-canary, non-self-pay, non-platform-verification buyer.

Known platform/test verifier wallets must remain excluded in `scripts/reconcile-treasury.mjs`. The 2026-09-07 receipt previously labeled `First Stranger Revenue` is superseded by `docs/growth-evidence-2026-09-10T0125+08-payapi-reclassification.md`: the same payer later produced an explicitly identified PayAPI paid verification call, so both same-wallet receipts are classified PayAPI platform verification rather than independent customer revenue. Until a different independently evidenced buyer settles, recognized customer revenue remains `$0.00`.

## Runtime acquisition in v0.6

- `/SKILL.md` — agent-consumable purchase instructions.
- `/distribution.json` — machine-readable distribution manifest.
- `/postman.json` — importable Postman collection.
- `/indexnow-key.txt` + scheduled IndexNow submission — automatic Bing/participating-engine URL notification.
- Daily Worker cron submits commercial/static pages only; customer proof pages remain `noindex` by default.
- Existing x402 Bazaar discovery metadata remains the primary machine-purchase path.

## Bootstrap rule

Agentic Market/Bazaar discovery may use external platform verification only when the platform funds its own verification call. One explicitly labeled project canary may be used only if a bootstrap remains necessary after every free publication surface is live; it must never be counted as customer revenue. Platform-funded verification likewise remains a technical settlement/treasury receipt but is never customer revenue.
