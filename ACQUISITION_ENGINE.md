# DELTA Acquisition Engine v0.6

> CURRENT OWNER PRICING (2026-09-10): Capture / authenticated Partner Capture / authenticated Partner Preflight / each NEW Watch check = **1 USDC**; Public Preflight = **5 USDC**; Guarded-Action Pilot = **10 USDC**. Read `AGENTS.md` and `docs/pricing-policy-2026-09-10.md` before every run. Conflicting old prices, automatic repricing and paid-canary instructions below are superseded. Historical receipts and prepaid obligations are not rewritten.

## Current revenue-first expansion handoff — 2026-09-18 08:45 JST

Read `docs/channel-status/revenue-expansion-2026-09-18-0845.json` before another prospect contact, reply, bid or registry action. It is the newest repository handoff and supplements, but does not replace, the canonical Commercial Operator workspace.

- Fresh independent Treasury evidence checked `2026-09-17T23:40:59.120Z`, covering Base blocks **51,349,551–51,449,550** (100,000 blocks), with **0 canonical-USDC Treasury receipt candidates** and **0 unclassified receipts**. Strict sprint count therefore remains **0 / 50** with **0 unique verified buyers**. This is explicit-range evidence, not an all-time zero claim; realized variable cost and contribution margin remain unknown/null, not zero.
- The one-shot 08:40 runner passed current owner pricing and live purchase-path checks before reconciliation. Current prices remain Capture **1 USDC**, Public Preflight **5 USDC**, Guarded-Action Pilot **10 USDC**, Base `eip155:8453`, canonical USDC and Treasury unchanged. Its current paid-funnel query did not run because Cloudflare credentials were unavailable in that one-shot runner; treat the 08:45 paid-funnel state as **unknown**, not zero. The last fresh canonical successful paid-funnel result recorded at the 07:54 handoff was `candidate_rows=0`.
- AgentPact still has the three canonical active offers at **1 / 5 / 10 USDC**, 16 adjacent external requests, **0 buyers**, **0 settlements** and **0 DELTA-native funded-compatible requests**. AgentExchange has 2 external requests and **0 compatible/funded-compatible requests**. Bountybook showed 13 adjacent funded requests, MoltJobs 10 funded rows and WorkPnP 0 funded rows; all had **0 DELTA-native funded-compatible requests**. PayanAgent returned a recoverable HTTP 500 and is not treated as proof of zero demand.
- Three independently qualified, deduped first contacts were sent to current official public business routes: **Sievo** (`contact@sievo.com`), **Sastrify** (`hello@sastrify.com`) and **ProcureDesk** (`sales@procuredesk.com`). Each ask is narrow: identify one real procurement/renewal/purchase workflow plus one representative public supplier/product/vendor URL, then start with one genuine buyer-paid **1 USDC Capture**; Public Preflight remains **5 USDC** only where an explicit expected-condition check is actually needed. These are **Grade-C commercial demand** until a reply/funding event exists; do not represent delivery as interest.
- **Outlast AI** remains blocked on its explicit company-domain sender prerequisite. Do not spoof or bypass. **Robauto / AgentHub** remains awaiting reply after DELTA answered the founder's use-case question; do not chase silence. **Reap** remains review-only acknowledgement, not buyer intent.
- The completed one-shot workflow `.github/workflows/tmp-revenue-run-20260918-0840.yml` was removed after evidence readback so it cannot become a duplicate growth/reconciliation loop.
- No fee, new cash/credit, wallet signature/message, asset movement, paid test, KYC/identity attestation, legal commitment, new seller identity or production deployment occurred in this run. Deadline stays **2026-09-21T13:43:28+09:00**.
- There is **no numerical daily outreach cap**. Scale only through independently qualified unique targets. Continue exact Gmail/operator dedupe, official public contacts only, one first contact per target, no chasing silence, and stop on rejection.

## Prior handoffs

All earlier handoffs under `docs/channel-status/revenue-expansion-2026-09-18-*.json` and `docs/channel-status/revenue-expansion-2026-09-17-*.json` remain audit evidence. In particular, `revenue-expansion-2026-09-18-0754.json` records GEP, JAGGAER APAC, Coupa Open Buy, Opstream, Zycus and Prokuria plus the last successful canonical paid-funnel query; `0509.json`, `0436.json`, `0330.json`, `0247.json` and `0152.json` preserve prior payment/procurement expansion, bounce corrections, Outlast/Robauto status and then-current reconciliations. Use the newest evidence when status conflicts and never rewrite historical receipts/exclusions.

North star: first **independent customer** payment, then repeatable CAC ≈ 0 channels.

Formula:

`Revenue = Discovery Surface × Qualified Intent × Payment Conversion × Delivery Success × Repeat Usage × ARPU`

The system expands Discovery Surface without adding sales labor.

## Revenue evidence invariant

A Base USDC transfer into Treasury is `treasury_received`, but it is not automatically customer revenue. Revenue is recognized only after the payer and DELTA proof/telemetry reconcile to a non-project, non-canary, non-self-pay, non-platform-verification buyer.

Known platform/test verifier wallets must remain excluded in `scripts/reconcile-treasury.mjs`. The 2026-09-07 receipt previously labeled `First Stranger Revenue` is superseded by `docs/growth-evidence-2026-09-10T0125+08-payapi-reclassification.md`: the same payer later produced an explicitly identified PayAPI paid verification call, so both same-wallet receipts are classified PayAPI platform verification rather than independent customer revenue. Until a different independently evidenced buyer settles, recognized customer revenue remains `$0.00`.

## Runtime acquisition in v0.6

- `/SKILL.md` — agent-consumable purchase instructions.
- `/distribution.json` — machine-readable distribution manifest.
- `/postman.json` — importable Postman collection.
- `/indexnow-key.txt` + scheduled IndexNow submission — automatic Bing/participating-engine URL notification.
- Daily Worker cron submits commercial/static pages only; customer proof pages remain `noindex` by default.
- Existing x402 Bazaar discovery metadata remains the primary machine-purchase path.

## Bootstrap rule

Agentic Market/Bazaar discovery may use external platform verification only when the platform funds its own verification call. One explicitly labeled project canary may be used only if a bootstrap remains necessary after every free publication surface is live; it must never be counted as customer revenue. Platform-funded verification likewise remains a technical settlement/treasury receipt but is never customer revenue.
